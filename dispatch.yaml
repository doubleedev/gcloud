dispatch:
  # Default service serves the typical web resources and all static resources.
  - url: "*/favicon.ico"
    service: default

  # Default service serves simple hostname request.
  - url: "689195055010@cloudbuild.gserviceaccount.com"
    service: default

  # Send all api related traffic to the tango_dev.
  - url: "*/api/*"
    service: api
  - url: "*/swagger/*"
    service: api
  - url: "*/hc"
    service: api
