dispatch:
  # Default service serves the typical web resources and all static resources.
  - url: "*/favicon.ico"
    service: default

  # Default service serves simple hostname request.
  - url: "doublee-sample-20210405-ei3he32sea-uc.a.run.app/"
    service: doublee-api

  # Send all api related traffic to the tango_dev.
  - url: "*/api/*"
    service: doublee-api
  - url: "*/swagger/*"
    service: doublee-api
  - url: "*/hc"
    service: doublee-api
