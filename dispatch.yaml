dispatch:
  # Default service serves the typical web resources and all static resources.
  - url: "*/favicon.ico"
    service: default

  # Default service serves simple hostname request.
  - url: "doublee-sample-20210405@appspot.gserviceaccount.com"
    service: default

  # Send all api related traffic to the tango_dev.
  - url: "*/api/*"
    service: api
  - url: "*/swagger/*"
    service: api
  - url: "*/hc"
    service: api
